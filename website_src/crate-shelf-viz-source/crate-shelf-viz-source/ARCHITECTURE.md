# 🏗️ Architecture & Component Hierarchy

## 📊 Application Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        Browser                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                   Next.js App                         │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │           Root Layout (layout.tsx)              │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │      MainLayout Component                 │  │  │  │
│  │  │  │  ┌─────────────┐  ┌──────────────────┐   │  │  │  │
│  │  │  │  │   Navbar    │  │   Page Content   │   │  │  │  │
│  │  │  │  └─────────────┘  │  (changes based  │   │  │  │  │
│  │  │  │  ┌─────────────┐  │   on route)      │   │  │  │  │
│  │  │  │  │   Sidebar   │  └──────────────────┘   │  │  │  │
│  │  │  │  └─────────────┘                          │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔀 Component Hierarchy

```
RootLayout (app/layout.tsx)
│
└── MainLayout (components/layout/MainLayout.tsx)
    │
    ├── Navbar (components/layout/Navbar.tsx)
    │   ├── Logo
    │   ├── Navigation Links
    │   └── Theme Toggle
    │
    ├── Sidebar (components/layout/Sidebar.tsx)
    │   ├── Home Link
    │   ├── Simulation Link
    │   ├── Dashboard Link
    │   └── About Link
    │
    └── Page Content (changes based on route)
        │
        ├── Home Page (app/page.tsx)
        │   ├── Hero Section
        │   ├── Feature Cards
        │   │   ├── Card (3D Simulation)
        │   │   ├── Card (Dashboard)
        │   │   └── Card (About)
        │   └── Info Section
        │
        ├── Simulation Page (app/simulation/page.tsx)
        │   ├── Page Header
        │   ├── ShelfScene (components/3d/ShelfScene.tsx)
        │   │   ├── Canvas (React Three Fiber)
        │   │   │   ├── Lights
        │   │   │   ├── Camera
        │   │   │   ├── OrbitControls
        │   │   │   ├── Environment
        │   │   │   ├── Grid
        │   │   │   ├── GroundPlane
        │   │   │   └── Crates (mapped from matrix)
        │   │   │       └── Crate (components/3d/Crate.tsx)
        │   │   │           ├── BoxGeometry
        │   │   │           └── MeshStandardMaterial
        │   │   └── Controls
        │   └── Control Panel
        │       ├── Matrix Input
        │       ├── Update Button
        │       └── Current Matrix Display
        │
        ├── Dashboard Page (app/dashboard/page.tsx)
        │   ├── Page Header
        │   ├── Stats Cards
        │   │   ├── Total Configurations
        │   │   ├── Simulations Run
        │   │   └── System Status
        │   └── Quick Actions
        │
        └── About Page (app/about/page.tsx)
            ├── Project Overview
            ├── Technology Stack
            ├── Key Features
            └── Team Section
```

---

## 🔄 Data Flow Diagram

### Simulation Page Data Flow

```
┌──────────────────────────────────────────────────────────────┐
│                     User Interaction                         │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  User inputs matrix: [[1, -1, 1], [-1, 1, -1]]              │
│  (in text input field)                                       │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  Click "Update" button                                       │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  handleUpdateMatrix() function                               │
│  - Parses JSON string                                        │
│  - Validates format                                          │
│  - Updates state: setMatrix(parsed)                          │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  State Change Triggers Re-render                             │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  ShelfScene receives new matrix prop                         │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  useMemo recalculates crate positions                        │
│  - Loops through matrix                                      │
│  - Calculates x, y, z for each crate                         │
│  - Determines color based on value                           │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  Crates array updated                                        │
│  [{id, position, color}, {id, position, color}, ...]         │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  React maps over crates array                                │
│  crates.map(crate => <Crate {...crate} />)                   │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  Each Crate component renders in 3D scene                    │
│  - Creates mesh at position                                  │
│  - Applies color                                             │
│  - Adds to Three.js scene                                    │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  WebGL renders the 3D scene                                  │
│  - User sees updated visualization                           │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎯 State Management Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  Simulation Page State                      │
│                                                             │
│  const [matrix, setMatrix] = useState(defaultMatrix)        │
│  const [matrixInput, setMatrixInput] = useState(string)     │
│                                                             │
└──────────────┬──────────────────────────────────────────────┘
               │
               ├─────────────────────────────────────────────┐
               │                                             │
               ▼                                             ▼
┌──────────────────────────┐              ┌─────────────────────────┐
│   Matrix Input Field     │              │    ShelfScene           │
│   - Displays matrixInput │              │    - Receives matrix    │
│   - onChange updates     │              │    - Renders 3D         │
│     matrixInput state    │              │                         │
└──────────────────────────┘              └─────────────────────────┘
               │
               ▼
┌──────────────────────────┐
│   Update Button          │
│   - onClick parses input │
│   - Updates matrix state │
└──────────────────────────┘
```

---

## 🧩 Component Relationships

### Parent-Child Relationships

```
MainLayout
├── Navbar (sibling to Sidebar and children)
├── Sidebar (sibling to Navbar and children)
└── children (page content)
    └── SimulationPage
        ├── Card (Matrix Input)
        │   ├── Input
        │   └── Button
        ├── Card (3D Visualization)
        │   └── ShelfScene
        │       └── Crate (multiple instances)
        └── Card (Current Matrix)
```

### Prop Drilling Example

```
SimulationPage
│   state: matrix = [[1, -1, 1]]
│
└── ShelfScene
    │   props: { matrix }
    │
    └── Crate (for each matrix element)
        props: { position, size, color }
```

---

## 🔧 3D Scene Hierarchy

```
Canvas (React Three Fiber wrapper)
│
├── Lights
│   ├── ambientLight (overall illumination)
│   ├── directionalLight (main light source)
│   └── pointLight (accent lighting)
│
├── Camera
│   └── position: [8, 6, 10]
│
├── Controls
│   └── OrbitControls (mouse interaction)
│
├── Environment
│   └── preset: "city" (HDR environment)
│
├── Scene Objects
│   ├── Grid (reference grid)
│   ├── GroundPlane (floor)
│   └── Crates (from matrix)
│       ├── Crate 1 (mesh)
│       │   ├── BoxGeometry
│       │   └── MeshStandardMaterial
│       ├── Crate 2 (mesh)
│       └── Crate N (mesh)
│
└── Renderer (WebGL)
```

---

## 📦 File Import/Export Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      Import Chain                           │
└─────────────────────────────────────────────────────────────┘

app/simulation/page.tsx
│
├── import ShelfScene from "@/components/3d/ShelfScene"
│   │
│   └── components/3d/ShelfScene.tsx
│       │
│       ├── import Crate from "./Crate"
│       │   └── components/3d/Crate.tsx
│       │
│       ├── import GroundPlane from "./GroundPlane"
│       │   └── components/3d/GroundPlane.tsx
│       │
│       └── import { Canvas, OrbitControls } from "@react-three/fiber"
│
├── import { Card } from "@/components/ui/card"
│   └── components/ui/card.tsx
│
├── import { Button } from "@/components/ui/button"
│   └── components/ui/button.tsx
│
└── import { Input } from "@/components/ui/input"
    └── components/ui/input.tsx
```

---

## 🎨 Styling Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   Tailwind CSS v4                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   app/globals.css                           │
│  - CSS variables (colors, spacing)                          │
│  - Theme definitions (light/dark)                           │
│  - Base styles                                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              Component-level Styling                        │
│  - Utility classes in className                             │
│  - Responsive modifiers (sm:, md:, lg:)                     │
│  - State modifiers (hover:, focus:)                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   shadcn/ui Components                      │
│  - Pre-styled with Tailwind                                 │
│  - Customizable via className                               │
│  - Consistent design system                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 Rendering Pipeline

```
1. User visits /simulation
   │
   ▼
2. Next.js loads app/simulation/page.tsx
   │
   ▼
3. React renders component tree
   │
   ▼
4. ShelfScene component mounts
   │
   ▼
5. React Three Fiber creates WebGL context
   │
   ▼
6. Three.js initializes scene, camera, renderer
   │
   ▼
7. Lights, controls, environment added
   │
   ▼
8. Crates created based on matrix
   │
   ▼
9. WebGL renders frame
   │
   ▼
10. Animation loop starts (60 FPS)
    │
    ▼
11. User interactions update scene
    │
    └──> Back to step 9 (continuous loop)
```

---

## 🔄 Update Cycle

```
User changes matrix input
│
▼
State updates (setMatrix)
│
▼
React re-renders SimulationPage
│
▼
ShelfScene receives new matrix prop
│
▼
useMemo recalculates crate positions
│
▼
New crates array generated
│
▼
React updates virtual DOM
│
▼
React Three Fiber updates Three.js scene
│
▼
WebGL re-renders with new crates
│
▼
User sees updated visualization
```

---

## 📱 Responsive Layout Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     Mobile (< 768px)                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Navbar (collapsed, hamburger menu)                   │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │  Content (full width, stacked vertically)             │  │
│  │  - 3D Scene (full width)                              │  │
│  │  - Controls (below scene)                             │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   Desktop (≥ 768px)                         │
│  ┌────────┬────────────────────────────────────────────────┐│
│  │        │  Navbar (full, expanded)                       ││
│  │        ├────────────────────────────────────────────────┤│
│  │ Side-  │  Content (grid layout)                         ││
│  │ bar    │  ┌──────────────┬──────────┐                   ││
│  │        │  │  3D Scene    │ Controls │                   ││
│  │        │  │  (2/3 width) │ (1/3)    │                   ││
│  │        │  └──────────────┴──────────┘                   ││
│  └────────┴────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Takeaways

1. **Component-Based**: Everything is a reusable component
2. **Props Down, Events Up**: Data flows down, events bubble up
3. **State Management**: Local state with useState
4. **3D Rendering**: React Three Fiber abstracts Three.js
5. **Responsive**: Tailwind handles all breakpoints
6. **Type-Safe**: TypeScript catches errors early

---

This architecture ensures:
- ✅ Maintainability
- ✅ Scalability
- ✅ Performance
- ✅ Developer Experience
- ✅ User Experience
